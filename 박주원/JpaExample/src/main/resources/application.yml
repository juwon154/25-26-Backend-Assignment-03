spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book_db
    username: book_db
    password: book_db
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    open-in-view: false
